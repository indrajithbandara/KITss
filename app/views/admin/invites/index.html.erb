<%= form_tag do %>
  <%= label_tag(:addresses, "Insert the email addreses, each one followed by a comma:") %>
  <%= text_area_tag(:addresses, @str, :size => "24x6") %>
  <%= submit_tag("Invite") %>
<% end %>
